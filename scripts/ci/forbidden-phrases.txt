# Denylist of facts a rename or a design decision invalidated. Read by
# check_contracts.py. Format (tab-separated): regex<TAB>reason<TAB>allow-glob(optional)
#
# allow-glob is a comma-separated list of fnmatch patterns (matched against the
# POSIX-style repo-relative path) for files where the phrase is a CORRECT
# statement (an attribution sentence, a documented DO-NOT list, a negated
# fact) rather than the dead claim this row exists to catch. Keep entries
# editable here rather than in code.
#
# Each allow-glob entry pins an EXPECTED OCCURRENCE COUNT for that phrase in
# that file: `path/glob:N` means exactly N legitimate occurrences are known to
# exist there; a bare `path/glob` (no `:N`) means exactly 1 (keeps terse rows
# valid). The file is still scanned -- a count ABOVE the pin is a finding
# naming the first excess occurrence (a new instance needs hand review, then
# the pin raised); a count BELOW the pin is a finding that the pin is stale
# (lower it). Counts below were computed with
# `grep -o '<phrase>' <file> | wc -l` against HEAD, not guessed.
PR body	/sdlc opens no PR; only correct when hedging about a hypothetical future PR-authoring step (the coauthor_trailer wording), never a claim that /sdlc itself writes one	templates/project.json.example:1,skills/repo-onboarding/SKILL.md:1
opens a PR	no skill in this repo opens a PR; correct only in the negated form ("never opens a PR" / "it never opens a PR")	skills/sdlc/SKILL.md:1,skills/repo-health/SKILL.md:2,skills/sdlc/templates/queue-mode.md:1,CLAUDE.md:1,AGENTS.md:1,docs/CONVENTIONS.md:1,docs/FLOW.md:1
gh pr create	/sdlc does no git writes; correct only inside the Stage 6 DO-NOT list naming the removed capability	skills/sdlc/SKILL.md:1,copilot/skills/sdlc/SKILL.md:1,codex/skills/sdlc/SKILL.md:1
commit_sha	only /task records a commit_sha -- it is the one skill that runs git commit	skills/task/SKILL.md:1
sanity_check\.model	dead key; renamed to models.sanity in the pipeline.*.model -> models.*/agents.* clean break	docs/MODEL-AXES.md:1
pipeline\.review_fix\.model	dead key; renamed to models.code_review	docs/MODEL-AXES.md:1
pipeline\.sanity_check\.focuses	dead key; renamed to agents.sanity_focuses	docs/MODEL-AXES.md:1
model-cap\.md	dead filename; the shared model-tier cap contract now lives at skills/sdlc/templates/models.md
sdlc-lite	/sdlc-lite was merged into /sdlc; the merged name never appears as a live reference inside skills/copilot/codex/agents/templates. Now that CLAUDE.md/AGENTS.md and docs/*.md are also scanned (check_contracts.py Step 2), the allowed occurrences there are attribution sentences that keep the OLD name by design (CLAUDE.md/AGENTS.md's own house-style example, docs/CONVENTIONS.md's Migration policy record) -- never a live claim that /sdlc-lite still exists.	CLAUDE.md:2,AGENTS.md:2,docs/CONVENTIONS.md:1
ux-plan-validator	dead agent name; renamed to plan-conformance-validator (it never validated UX -- it checks plan conformance on requirements + flow). Historical references in docs/archive and docs/gap-analysis are deliberate records and are out of this scan scope.
no stash, commit, checkout, switch	the sub-agent git-write guard: pinned so removing it from a dispatch prompt fails CI	skills/sdlc/templates/stage-2-implement.md:1,skills/sdlc/templates/stage-2b-dispatch.md:1,skills/sdlc/templates/fix-loop.md:1,skills/sdlc/templates/stage-5.7-review-fix.md:1,skills/sdlc/templates/stage-5.9-cleanup.md:1,agents/e2e-test-runner.md:1
never reference the plan in code	the COMMENTS guard carried in every code-writing prompt: pinned so deleting it from a dispatch prompt fails CI	agents/e2e-test-runner.md:1,skills/docstring-sync/references/rewrite-rules.md:1,skills/sdlc/templates/fix-loop.md:1,skills/sdlc/templates/stage-2-implement.md:1,skills/sdlc/templates/stage-2b-dispatch.md:1,skills/sdlc/templates/stage-2c-converge.md:1,skills/sdlc/templates/stage-5.7-review-fix.md:1,skills/sdlc/templates/stage-5.9-cleanup.md:1,skills/task/SKILL.md:1
